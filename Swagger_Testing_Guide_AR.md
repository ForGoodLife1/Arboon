# دليل اختبار الواجهة البرمجية (API) باستخدام Swagger لمبرمجي الباك إند

هذا الملف يشرح الخطوات الأساسية لإدخال البيانات واختبار جميع مسارات واجهة برمجة التطبيقات (Endpoints) الخاصة بمشروع Arboon باستخدام Swagger.

## 1. الوصول إلى واجهة Swagger
- تأكد من تشغيل المشروع (المشروع يعمل حالياً باستخدام أمر `dotnet run`).
- افتح المتصفح وانتقل إلى الرابط: `http://localhost:5000/swagger` أو `https://localhost:5001/swagger` (أو البورت الذي يظهر لك في الـ Terminal).
- ستظهر لك واجهة Swagger تحتوي على جميع الـ Controllers المتاحة في النظام.

## 2. المصادقة (Authentication) 
*(ملاحظة: إذا تم إيقاف قيود المصادقة لتسهيل التطوير، يمكنك تخطي هذه الخطوة، ولكن هذه هي الطريقة القياسية)*
- ابحث عن مسار تسجيل الدخول (`POST /api/auth/login`).
- قم بإدخال بيانات المستخدم (الإيميل وكلمة المرور).
- بعد نجاح الطلب، انسخ رمز الدخول (Token) من الـ Response.
- اذهب إلى أعلى صفحة Swagger واضغط على زر **"Authorize"** (علامة القفل).
- اكتب كلمة `Bearer` متبوعة بمسافة ثم الصق الـ Token (مثال: `Bearer eyJhbGciOi...`).
- اضغط على **"Authorize"** ثم **"Close"**. الآن جميع الطلبات القادمة ستكون مصادقاً عليها.

## 3. خطوات الاختبار المتسلسلة (Endpoints Testing Flow)

لضمان اختبار النظام بشكل صحيح، يجب اتباع تسلسل منطقي لأن بعض الـ Endpoints تعتمد على بيانات من Endpoints أخرى.

### أ. تهيئة المستخدمين والمحافظ (Users & Wallet)
1. **إنشاء المستخدمين (`POST /api/auth/register`)**:
   - قم بإنشاء مستخدمين على الأقل (أحدهما سيكون المشتري والآخر البائع).
   - **احتفظ بـ `userId` لكل منهما من الـ Response في ملف جانبي.**
2. **شحن المحفظة (`POST /api/wallet/deposit`)**:
   - أضف رصيد لمحفظة المشتري ليتمكن من إنشاء المعاملات ودفع قيمتها.
   - أدخل قيمة المبلغ المراد شحنه.
3. **الاستعلام عن الرصيد (`GET /api/wallet/balance`)**:
   - استخدمه للتأكد من أن الرصيد قد أُضيف بنجاح لمُعرّف المشتري.

### ب. دورة حياة الوساطة (Escrow Lifecycle)
1. **إنشاء معاملة جديدة (`POST /api/escrows`)**:
   - كمشتري، قم بإنشاء معاملة جديدة.
   - **البيانات المطلوبة**: عنوان المعاملة، الوصف، المبلغ، ومعرف البائع (`sellerId` الذي حفظته مسبقاً).
   - **احتفظ بـ `escrowId` الناتج من الـ Response.**
2. **تحديث حالة المعاملة**:
   - **تمويل المعاملة (`POST /api/escrows/{id}/fund`)**: إذا كان النظام يتطلب تمويلها كخطوة منفصلة.
   - **شحن البضاعة (`POST /api/escrows/{id}/ship`)**: يقوم البائع بتحديث الحالة للإشارة إلى شحن أو تسليم الخدمة.
   - **تحرير المبلغ (`POST /api/escrows/{id}/release`)**: يقوم المشتري بتأكيد الاستلام، مما يؤدي إلى تحويل المبلغ لمحفظة البائع.
   - **إلغاء المعاملة (`POST /api/escrows/{id}/cancel`)**: جرب هذه العملية في معاملة منفصلة لاختبار استرجاع المبلغ للمشتري.

### ج. نظام النزاعات (Dispute System)
1. **فتح نزاع (`POST /api/disputes`)**:
   - أنشئ معاملة (Escrow) جديدة، ثم استخدم الـ `escrowId` الخاص بها لفتح نزاع قبل تحرير المبلغ.
   - أدخل سبب النزاع وتفاصيله.
   - **احتفظ بـ `disputeId` الناتج.**
2. **التراسل داخل النزاع (`POST /api/disputes/{id}/messages`)**:
   - استخدم `disputeId` لإضافة رسائل من كلا الطرفين (المشتري والبائع) لاختبار الشات الخاص بالنزاع.
3. **عرض النزاع (`GET /api/disputes/{id}`)**:
   - استدعِ هذه الـ Endpoint للتأكد من ظهور تفاصيل النزاع وجميع الرسائل المرفقة.

### د. إدارة المشرفين للنزاعات (Admin Disputes)
1. **حل النزاع (`POST /api/admindisputes/{id}/resolve`)**:
   - هذه العملية تحاكي تدخل الإدارة.
   - أدخل `disputeId`.
   - حدد في الـ Body الـ `winnerId` (معرف المشتري أو البائع الذي سيكسب النزاع) وملاحظات الحل (`resolutionNotes`).
   - تحقق بعد ذلك من محفظة الطرف الفائز لتتأكد من انتقال المبلغ إليه.

### هـ. نظام الإشعارات (Webhooks)
1. **التسجيل (`POST /api/webhooks`)**:
   - أضف URL وهمي (مثل رابط من موقع `webhook.site`) لاستقبال الإشعارات.
   - حدد نوع الأحداث المطلوبة، مثل: `escrow.created` أو `dispute.opened`.
2. **الاختبار الفعلي**:
   - بعد تسجيل الـ Webhook، اذهب وأنشئ Escrow جديد أو افتح نزاعاً.
   - راقب موقع الـ URL الذي استخدمته، يجب أن يصلك Request يحتوي على بيانات (Payload) تشرح الحدث الذي تم للتو.

## 4. نصائح هامة للمطور أثناء الاختبار
- **مفكّرة خارجية (Notepad)**: نظراً لاعتماد النظام المالي والوساطة على معرّفات (UUIDs)، افتح ملف نصي جانبي وانسخ فيه أي `id` يظهر لك (UserId للمشتري، UserId للبائع، EscrowId، DisputeId). سيوفر هذا عليك الكثير من الوقت بدلاً من البحث في قوائم (GET) للحصول عليها مرة أخرى.
- **تحليل رسائل الخطأ (Status Codes)**:
  - `200/201 (Success)`: تم بنجاح.
  - `400 (Bad Request)`: لديك خطأ في الـ JSON الذي أرسلته (مثلاً نسيت حقلاً مطلوباً أو أرسلت String بدلاً من الرقم). اقرأ الرسالة في الـ Response بدقة.
  - `404 (Not Found)`: الـ ID الذي أدخلته غير موجود (تأكد من نسخه بالكامل بدون فراغات إضافية).
  - `500 (Server Error)`: خطأ في الكود أو الداتا بيز. عند حدوثه، انتقل إلى الـ Terminal الذي يعمل عليه أمر `dotnet run` واقرأ الـ Exception لمعرفة سبب الخطأ بالضبط.
